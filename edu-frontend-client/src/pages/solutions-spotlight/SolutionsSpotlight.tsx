import { Box } from "@mui/material";
import { Outlet } from "react-router-dom";

const SolutionsSpotlight = () => {
  return (
    <Box>
      <Outlet />
    </Box>
  );
};

export default SolutionsSpotlight;
